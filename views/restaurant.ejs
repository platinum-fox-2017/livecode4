<!doctype html>
<html>
    <head>

    </head>
    <body>
        <header>

        </header>
        <div>
            <table>
                <tr>
                    <td>Id</td>
                    <td>Name</td>
                    <td>Address</td>
                    <td>Action</td>
                </tr>
                <% for (let i = 0; i < restaurant.length; i++){ %>
                    <tr>
                        <td><%= restaurant[i].id %></td>
                        <td><%= restaurant[i].name %></td>
                        <td><%= restaurant[i].address %></td>
                        <td><button><a href = '/restaurants/<%= restaurant[i].id %>/view_menu'>View Menu</a></button></td>
                    </tr>
                <% } %>
            </table>
        </div>
    </body>
</html>
