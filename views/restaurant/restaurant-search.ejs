<!doctype html>

<head>
  <meta charset="utf-8">
  <title>Restaurant</title>

  <link rel="stylesheet" href="/css/style.css">
</head>
 <body>
     <h3>Menu Restaurant <%=restaurant.name%></h3>
     <form action='/restaurants/<%=restaurant.id%>/search' method='get'>
    <p>Search by:
    <select name='search_type'>
        <option value="name">name</option>
        <option value="menu_type">menu_type</option>
        <option value="rating">rating</option>
        <option value="price">price</option>
    </select>
    Keyword:
    <input type='text' name='search'></p>
    <input type='submit' value='Submit'>
    </form>
     <table>
         <tr>
             <th>Id</th>
             <th>Name</th>
             <th>Menu Type</th>
             <th>Rating</th>
             <th>Price</th>
         </tr>
         <%for (let i = 0; i < menus.length; i++){%>
             <tr>
                 <td><%=menus[i].id%></td>
                 <td><%=menus[i].name%></td>
                 <td><%=menus[i].menu_type%></td>
                 <td><%=menus[i].rating%></td>
                 <td><%=helper.format_currency(menus[i].price)%></td>
             </tr>
        <% }%>


     </table>
 </body>
